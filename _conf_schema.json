{
  "group_list_mode": {
    "description": "群聊权限模式",
    "type": "string",
    "options": ["whitelist", "blacklist", "none"],
    "default": "none",
    "hint": "whitelist: 仅允许列表内群；blacklist: 拒绝列表内群；none: 不限制"
  },
  "group_list": {
    "description": "群号列表（填 QQ 群号）",
    "type": "list",
    "items": { "type": "string" },
    "default": [],
    "hint": "仅在 whitelist/blacklist 下生效；留空等效于无限制"
  },
  "enable_video_explain": {
    "description": "是否启用视频解释（抽帧+可选 ASR）",
    "type": "bool",
    "default": true,
    "hint": "启用后识别视频并进行解释，需 ffmpeg 支持"
  },
  "video_frame_interval_sec": {
    "description": "抽帧间隔（秒）",
    "type": "int",
    "default": 6,
    "hint": "每隔 N 秒取 1 帧"
  },
  "video_asr_enable": {
    "description": "是否启用音频转写（ASR）",
    "type": "bool",
    "default": false,
    "hint": "调用 AstrBot 已配置的语音识别提供商（STT）"
  },
  "asr_provider_id": {
    "description": "ASR 提供商 ID（语音识别）",
    "type": "string",
    "_special": "select_provider_stt",
    "hint": "可选；留空则使用当前会话 STT 提供商"
  },
  "video_max_duration_sec": {
    "description": "最大允许视频时长（秒）",
    "type": "int",
    "default": 120,
    "hint": "超过则拒绝并提示"
  },
  "video_max_size_mb": {
    "description": "最大允许视频大小（MB）",
    "type": "int",
    "default": 50,
    "hint": "超过则拒绝并提示"
  },
  "ffmpeg_path": {
    "description": "ffmpeg 可执行文件路径",
    "type": "string",
    "default": "ffmpeg",
    "hint": "可填写绝对路径；若为空将自动查找或尝试 imageio-ffmpeg"
  },
  "text_provider_id": {
    "description": "文本解释优先使用的 Provider ID",
    "type": "string",
    "_special": "select_provider",
    "hint": "可选；留空则使用当前会话 Provider"
  },
  "image_provider_id": {
    "description": "图片转述优先使用的 Provider ID",
    "type": "string",
    "_special": "select_provider",
    "hint": "可选；留空则自动选择具备图片能力的 Provider"
  },
  "video_provider_id": {
    "description": "视频解释优先使用的 Provider ID（建议支持图片输入）",
    "type": "string",
    "_special": "select_provider",
    "hint": "可选；仅用于视频解释，不影响图片转述"
  },
  "enable_url_detect": {
    "description": "是否识别 URL 并输出网页简版摘要（2-8句）",
    "type": "bool",
    "default": true,
    "hint": "关闭后仅按文本/图片解释"
  },
  "url_timeout_sec": {
    "description": "抓取网页超时时间（秒）",
    "type": "int",
    "default": 20,
    "hint": "范围建议 2-60"
  },
  "url_max_chars": {
    "description": "参与摘要的正文片段最大长度（字符）",
    "type": "int",
    "default": 4000,
    "hint": "限制输入规模，避免过长上下文"
  }
}
